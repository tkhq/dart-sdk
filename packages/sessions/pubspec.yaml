name: turnkey_sessions
description: A Dart package for managing sessions in secure storage
version: 0.1.4
repository: https://github.com/tkhq/dart-sdk

environment:
  sdk: ^3.6.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  turnkey_crypto: ^0.2.0
  turnkey_encoding: ^0.1.1
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
